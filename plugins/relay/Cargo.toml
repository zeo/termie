[package]
name = "relay"
version = "0.1.0"
edition = "2024"

# own workspace table so this is independent of termie's build
[workspace]

[[bin]]
name = "relay"
path = "main.rs"

[profile.release]
opt-level = 3
strip = true
panic = "abort"
